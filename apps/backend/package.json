{
	"name": "backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "air",
		"start": "go run .",
		"build": "go build -o build/main .",
		"test": "go test ./...",
		"lint": "golangci-lint run ./...",
		"lint:fix": "golangci-lint run --fix ./...",
		"db:migrate": "go run ./internal/migrations",
		"install": "go mod tidy"
	}
}
